<atv>
    <head>
        <script src="{{URL(:/js/utils.js)}}" />
    </head>
    {{ADDXML(music::/library/sections/8/all)}}
    <body>
        <scroller id="com.sample.movie-grid">
            {{CUT(size:CUT:0=CUT|1=)}}
      
            <items>
                <grid columnCount="7" id="grid_0">
                     {{CUT(@music/size:CUT:0=CUT|1=)}}
                    <items>
                        <squarePoster id="{{VAL(key)}}" alwaysShowTitles="{{VAL($showtitles_movies::Highlighted Only=false|Show All=true)}}"
                            onPlay="atv.loadURL('{{URL(key)}}&amp;PlexConnect=Play')"
                            onSelect="atv.loadURL('{{URL(key)}}&amp;PlexConnect=MoviePrePlay')"
                            onHoldSelect="scrobbleMenu('{{URL(key)}}&amp;PlexConnect=ScrobbleMenuVideo')">
                            {{COPY(@music/Directory)}}
                            {{VAR(items:NoKey:TRUE)}}
                            <title>{{VAL(title)}}</title>
                            <subtitle>{{VAL(year:&#x00AD;)}}</subtitle>
                            <image>{{IMAGEURL(thumb::384)}}</image>
                            <defaultImage>resource://Poster.png</defaultImage>
                        </squarePoster>
                    </items>
                </grid>
            </items>
        </scroller>
        
        <!--No Items Available-->
        <dialog id="com.sample.error-dialog">
            {{CUT(size::0=|1=CUT)}}
            <title>PlexConnect</title>
            <description>{{TEXT(Plex Media Server: No Items Available)}}</description>
        </dialog>
        
    </body>
</atv>
